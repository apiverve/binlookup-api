name: apiverve_binlookup
description: BIN Lookup is a simple tool for looking up BIN number information. It returns information such as the bank, card type, and more based on the BIN number provided.
version: 1.1.13
homepage: https://apiverve.com/marketplace/binlookup?utm_source=dart&utm_medium=homepage
repository: https://github.com/apiverve/binlookup-api/tree/main/dart

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  http: ^1.1.0
  http_parser: ^4.0.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
